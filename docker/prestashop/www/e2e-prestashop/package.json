{
    "name": "woocommerce-e2e",
    "devDependencies": {
        "@playwright/test": "^1.42.1"
    },
    "scripts": {
        "test": "playwright test",
        "test:payments": "EPAYCO_FLOW=${FLOW:-cash}  BASE_URL=${BASE_URL:-http://localhost:8083} playwright test --reporter=line --workers=1 --repeat-each=${TX_COUNT:-1}"
    }
}
